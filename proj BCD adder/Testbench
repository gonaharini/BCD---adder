`timescale 1ns/1ps

module bcd_adder_tb;

reg [3:0] A;
reg [3:0] B;
reg Cin;

wire [3:0] Sum;
wire Cout;

bcd_adder uut(
    .A(A),
    .B(B),
    .Cin(Cin),
    .Sum(Sum),
    .Cout(Cout)
);

initial begin

$display("A B Cin | Sum Cout");

A=4'd3; B=4'd4; Cin=0;
#10;
$display("%d %d %d | %d %d",A,B,Cin,Sum,Cout);

A=4'd5; B=4'd7; Cin=0;
#10;
$display("%d %d %d | %d %d",A,B,Cin,Sum,Cout);

A=4'd9; B=4'd9; Cin=0;
#10;
$display("%d %d %d | %d %d",A,B,Cin,Sum,Cout);

A=4'd8; B=4'd2; Cin=1;
#10;
$display("%d %d %d | %d %d",A,B,Cin,Sum,Cout);

A=4'd6; B=4'd3; Cin=0;
#10;
$display("%d %d %d | %d %d",A,B,Cin,Sum,Cout);

$finish;

end

endmodule